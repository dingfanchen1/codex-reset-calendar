# 阶段窗口启动提示词

以下提示词可独立用于阶段窗口；不需要附带整个历史聊天。首次创建只读待命，示例启动口令不是用户已经要求实施。

## S1｜本地转换与自动化测试

你负责本项目 S1。共用当前项目目录，不创建工作树，不读取整段历史聊天。首先阅读 AGENTS.md、README.md、Repo_Current_State.md、Tickets.md 的 S1、Development_Plan.md 第 1–4 与第 6 节、Manual_Verification_Guide.md 的 S1。

本条是建窗说明：只读检查 Git 状态及规范是否已提交，报告前置条件，然后结束待命。不得写文件、安装依赖、提交、创建远程或启动实现。用户后续发送“开始 S1”才进入开发。

启动后仅实现 Python/icalendar 同步与当前事件 ICS、稳定 UID 和版本、清空与失败保留、两个提醒、固定样例和 unittest。规范是唯一技术基线，不拓展历史、X API 或 Mac App。核验输入不变时输出字节相同、异常不破坏旧输出，运行 unittest 并另外核对一次实时上游。只修改本阶段文件，登记实施者，完成后同步文档、提交明确文件并报告提交号，清空实施者，不启动 S2。

## S2｜自动化工作流与发布准备

你负责本项目 S2。共用当前目录，不创建工作树，不继承长聊天。首先阅读 AGENTS.md、README.md、Repo_Current_State.md、Tickets.md 的 S2、Development_Plan.md 第 5–6 节及必要输入/输出规范、Manual_Verification_Guide.md 的 S2/S3。

本条仅建窗：只读检查 S1 是否已验收并提交，报告前置条件后结束待命。不得实现、写文件、安装、提交、配置远程或发起部署。用户后续发送“开始 S2”且 S1 通过后才实施。

负责定时/手动/分支/PR 工作流、变化提交、明确 Pages 部署、线上比较、失败恢复、最小权限及串行、生产测试隔离。完成静态检查及本地模拟首次/无变化/不匹配/失败恢复，运行相关回归。停止于发布准备完成，云端仍待验证。登记/清空实施者，同步文档，本地提交并报告，不启动 S3、不推送。

## S3｜GitHub Pages 发布与线上验收

你负责本项目 S3。共用当前目录，不创建工作树。先读 AGENTS.md、README.md、Repo_Current_State.md、Tickets.md 的 S3、Development_Plan.md 第 5–6 节、Manual_Verification_Guide.md 的 S3。

本条只建窗：只读检查 S2 验收和本地 Git 状态，报告前置条件后结束待命。不得创建/修改远程、推送或部署，不索要授权来抢跑。后续用户“开始 S3”授权准备检查；公开仓库和推送仍须明确授权。

启动后先完成公开文件清单、账号、分支与提交检查，让发布动作具体可审阅。已有明确授权则不重复问；没有授权则说明必要的具体授权。发布后验证公共 ICS 的 200/类型/编码/解析/内容一致、手动及真实定时运行、无变化不重复和失败恢复，记录实际 URL 与运行链接。实机待 S4，不伪造实时/设备结果。交接文档和本地提交后结束，不启动 S4。

## S4｜iPhone 订阅与双提醒实机验收

你负责本项目 S4。共用当前目录，不创建工作树。先读 AGENTS.md、README.md、Repo_Current_State.md、Tickets.md 的 S4、Manual_Verification_Guide.md 的 S4 和 Development_Plan.md 第 4、6 节。

本条只建窗：只读检查 S3 是否有公共地址及服务验收证据，报告前置条件后结束待命。不得发布测试数据、操作日历或修改文件；用户后续“开始 S4”且 S3 通过后才实施。

引导真实订阅而非导入；使用独立测试源验证同 UID 改期、新 UID 替换、清空和至少两小时后的双提醒。记录设备/iOS、发布和刷新时刻、两次真实通知证据，保护其他私人日程。生产源不混入假事件。缺少用户设备操作时停在待验收，不用生成成功替代实机。必要修复仅限本阶段验证出的相关问题，回归后重新验收；同步文档并本地提交，报告通过项与未通过项。

## 通用后续口令

用户在对应窗口发送“开始 S1”“开始 S2”“开始 S3”或“开始 S4”。开始前重新读取当前仓库状态，不能沿用建窗时的旧快照。任一前置未通过或其他窗口正在实施，先说明原因，不并发修改。
