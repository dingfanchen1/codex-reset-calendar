# 项目协作规则

## 用户与沟通

- 默认中文，面向 AI 编程学习者；必要术语解释含义，关键步骤说明原因。
- 复杂工作先给简短计划和验证方式；关键方向不明确时先确认，不静默改变需求。
- 简单优先，不实现未要求的功能，不顺手重构、格式化或清理无关内容。
- 不把规划、实现、测试、部署、实机验收混称完成；只报告有证据的状态。

## 启动必读

实施前阅读 README.md、本文、Tickets.md、Repo_Current_State.md、Manual_Verification_Guide.md、Prompt_Playbook.md，以及 Development_Plan.md 中本阶段相关章节。缺失时说明并恢复所需规范，不猜测历史。

窗口首次创建只允许读取这些文件和 Git 状态，报告前置条件后结束待命。初始建窗消息中的“开始 S1”等示例不是实际启动指令。收到用户后续明确启动本阶段的消息，才可实施。

## 共用目录与串行开发

- 四阶段直接使用当前项目目录，依赖顺序 S1 → S2 → S3 → S4。不要自行建立工作树或更换目录。
- 一次只允许一个阶段实施。开始时检查工作区、当前实施者及前置验收；发现其他窗口的修改时不覆盖、不回退，先说明。
- 在 Repo_Current_State.md 登记当前实施阶段，再实施；结束后清空实施者并记录交接。此登记是串行约定，不声称能替代并发锁。
- 不自动推进下一阶段，不代用户在其他窗口发送开发启动指令。
- 新窗口不需要完整历史聊天，以仓库规范、当前代码及前置阶段证据为准。

## 修改、删除与依赖

- 每处变动必须直接服务于当前任务，匹配已有风格；仅清理本次引入的无用内容。
- 禁止递归、强制、通配符或批量删除；禁止 rm -rf 等高风险命令。删除单个文件前说明确切路径、理由和运行影响；不删除用户资料或目录。
- 不引入大型依赖或擅自替换技术栈。icalendar 是已批准方案中的依赖，使用前核实稳定版本、许可证及维护成本并固定版本。
- 优先复用成熟方案，保留必要版权、许可证和来源；授权不明时明确说明，不复制未知许可证的大段代码。
- 不泄露、硬编码或提交密钥、Token、密码、Cookie、私钥；示例只用占位符。

## Git 与发布

- 本轮已授权建立本地基线；各阶段完成时可提交当前阶段明确文件，执行前说明命令及影响。
- 使用显式路径暂存，不使用 git add . 或 git add -A，不混入其他窗口改动。
- 不擅自推送、创建公开仓库或配置发布。S3 必须有明确公开仓库及推送授权；单纯“开始 S3”只授权准备和检查，不自动授权公开。
- 不强制推送、不回退其他人修改、不为解决冲突批量删除文件。
- 不在提交消息中写入密钥、隐私或无关信息。

## 验收与交接

- 运行本阶段相关测试，必要时回归；未实现测试不伪造运行结果。
- 保持代码、README、任务、当前状态和手动验收说明一致。
- 阶段结束按“目标、修改内容、验证方式与结果、风险与未完成项、一个下一步”汇报。
- Repo_Current_State.md 留下完成内容、证据、限制及下一阶段输入。提交后在窗口最终消息给出提交号；下一阶段用 Git 核对，避免文档自引用提交号。
- 本地通过、云端通过和实机通过分别记录；缺少设备操作时保持待验收。
